name: Session Cron Runner

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Scheduler disabled
        run: echo "Production scheduling is owned by cron-job.org. GitHub Actions cron invocations are disabled."
