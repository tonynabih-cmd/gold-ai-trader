# Frontend Samples

These React components demonstrate the dashboard structure for a public-safe trading monitor. They use mock props and do not connect to real APIs, broker services, databases, or live account data.

The production dashboard, private fields, account details, and operational controls are intentionally excluded.

